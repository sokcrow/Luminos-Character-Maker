(function(root,factory){const api=factory(root);if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root)root.LuminousVttPhysicalResolver=api;})(typeof window!=='undefined'?window:globalThis,function(root){'use strict';
const EPS=1e-7,DEFAULT_TOKEN_HEIGHT_FT=6,DEFAULT_EYE_HEIGHT_FT=5.5,COVER_LEVELS=Object.freeze(['none','partial','major','full']),COVER_RANK=Object.freeze({none:0,partial:1,major:2,full:3});
const finite=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f,clean=v=>String(v??'').trim().toLowerCase(),clone=v=>v==null?v:JSON.parse(JSON.stringify(v));
function coreRuntime(){if(root?.LuminousVttWorldObjectCore)return root.LuminousVttWorldObjectCore;if(typeof require!=='undefined'){try{return require('./world-object-core.js');}catch(_){}}return null;}
function lightingRuntime(){if(root?.LuminousVttLightingEngine)return root.LuminousVttLightingEngine;if(typeof require!=='undefined'){try{return require('./lighting-engine.js');}catch(_){}}return null;}
function layerOf(entity={}){if(Number.isFinite(Number(entity.position?.zLayer)))return Number(entity.position.zLayer);if(Number.isFinite(Number(entity.zLayer)))return Number(entity.zLayer);if(Number.isFinite(Number(entity.gridPosition?.z)))return Number(entity.gridPosition.z);if(Array.isArray(entity.z)&&entity.z.length)return Number(entity.z[0])||0;return lightingRuntime()?.layerOf?.(entity)||0;}
function feetPerPixel(mapData={}){return lightingRuntime()?.feetPerPixel?.(mapData)||Math.max(.001,finite(mapData.grid?.distancePerCell,5))/Math.max(1,finite(mapData.grid?.size,70));}
function layerElevationFt(mapData={},zLayer=0){const l=lightingRuntime();if(l?.elevationForLayer)return l.elevationForLayer(mapData,zLayer);const rec=mapData.zLevels?.[String(zLayer)]||mapData.zLevels?.[zLayer];return Number.isFinite(Number(rec?.elevationFt))?Number(rec.elevationFt):finite(zLayer)*finite(mapData.defaultZStepFt,15);}
function entityBaseElevationFt(entity={},mapData={}){const base=layerElevationFt(mapData,layerOf(entity));if(entity.position&&Number.isFinite(Number(entity.position.elevationFt)))return base+Number(entity.position.elevationFt);if(Number.isFinite(Number(entity.elevationFt)))return Number(entity.elevationFt);return base;}
function tokenHeightFt(token={}){const value=[token.heightFt,token.bodyHeightFt,token.physicalHeightFt,token.actor?.heightFt,token.raw?.heightFt].find(v=>Number.isFinite(Number(v))&&Number(v)>0);return value==null?DEFAULT_TOKEN_HEIGHT_FT:Number(value);}
function eyeHeightFt(token={},mapData={}){const rel=Number.isFinite(Number(token.eyeHeightFt))?Math.max(0,Number(token.eyeHeightFt)):Math.min(tokenHeightFt(token),DEFAULT_EYE_HEIGHT_FT);return entityBaseElevationFt(token,mapData)+rel;}
const tokenTopFt=(token={},mapData={})=>entityBaseElevationFt(token,mapData)+tokenHeightFt(token),objectBaseFt=(instance={},mapData={})=>entityBaseElevationFt(instance,mapData),objectTopFt=(instance={},def={},mapData={})=>objectBaseFt(instance,mapData)+Math.max(0,finite(def?.physical?.heightFt));
function definitionsMap(mapData={}){return{...(root?.LuminousVttWorldObjectCatalog?.byId||{}),...(mapData.worldObjectDefinitions||{})};}function definitionFor(instance,mapData={}){return instance?coreRuntime()?.resolveDefinition?.(instance,definitionsMap(mapData))||null:null;}
function pointInsideRect(point={},rect={}){const x=finite(point.x,NaN),y=finite(point.y,NaN);return Number.isFinite(x)&&Number.isFinite(y)&&x>=finite(rect.x)-EPS&&x<=finite(rect.x)+finite(rect.width)+EPS&&y>=finite(rect.y)-EPS&&y<=finite(rect.y)+finite(rect.height)+EPS;}
function segmentRectInterval(from={},to={},rect={}){const x0=finite(from.x,NaN),y0=finite(from.y,NaN),x1=finite(to.x,NaN),y1=finite(to.y,NaN);if(![x0,y0,x1,y1].every(Number.isFinite))return null;const minX=finite(rect.x),maxX=minX+Math.max(0,finite(rect.width)),minY=finite(rect.y),maxY=minY+Math.max(0,finite(rect.height)),dx=x1-x0,dy=y1-y0;let t0=0,t1=1;const clip=(p,q)=>{if(Math.abs(p)<EPS)return q>=-EPS;const r=q/p;if(p<0){if(r>t1)return false;if(r>t0)t0=r;}else{if(r<t0)return false;if(r<t1)t1=r;}return true;};if(!clip(-dx,x0-minX)||!clip(dx,maxX-x0)||!clip(-dy,y0-minY)||!clip(dy,maxY-y0)||t1<0||t0>1||t0>t1)return null;return{enter:Math.max(0,t0),exit:Math.min(1,t1)};}
function rayElevationFt(entity={},mapData={},mode='point'){if(mode==='viewer'||Number.isFinite(Number(entity.eyeHeightFt)))return eyeHeightFt(entity,mapData);if(Number.isFinite(Number(entity.rayElevationFt)))return Number(entity.rayElevationFt);if(Number.isFinite(Number(entity.elevationFt)))return Number(entity.elevationFt);return entityBaseElevationFt(entity,mapData);}
const occlusionKind=def=>clean(def?.physical?.occlusion||'opaque')||'opaque';function occlusionBlocks(def={},kind='vision'){const mode=occlusionKind(def);if(['none','transparent','open'].includes(mode))return false;if(kind==='light'&&mode==='vision_only')return false;if(kind==='vision'&&mode==='light_only')return false;return true;}
function objectOccludersBetween(from={},to={},mapData={},kind='vision'){const core=coreRuntime();if(!core||layerOf(from)!==layerOf(to))return[];const fromZ=rayElevationFt(from,mapData,Number.isFinite(Number(from.eyeHeightFt))?'viewer':'point'),toZ=rayElevationFt(to,mapData,Number.isFinite(Number(to.eyeHeightFt))?'viewer':'point'),hits=[];for(const instance of mapData.worldObjects||[]){if(core.objectLayer(instance)!==layerOf(from)||core.isDestroyed(instance))continue;const def=definitionFor(instance,mapData);if(!def||!occlusionBlocks(def,kind))continue;const rect=core.footprintRect(instance,def,mapData.grid||{}),interval=segmentRectInterval(from,to,rect);if(!interval)continue;const t=(interval.enter+interval.exit)/2;if(t<=.01||t>=.99)continue;const rayFt=fromZ+(toZ-fromZ)*t,baseFt=objectBaseFt(instance,mapData),topFt=objectTopFt(instance,def,mapData);if(rayFt+EPS<baseFt||rayFt-EPS>topFt)continue;hits.push({instance,definition:def,rect,interval,rayFt,baseFt,topFt,t});}return hits.sort((a,b)=>a.t-b.t);}
const blocksLineOfEffect=(from={},to={},mapData={},kind='vision')=>objectOccludersBetween(from,to,mapData,kind).length>0;
function qualityLevel(v){const q=clean(v);if(q==='full')return'full';if(['major','three_quarters','three-quarters','heavy'].includes(q))return'major';if(['partial','half','light'].includes(q))return'partial';return'none';}
function inferredCoverLevel(instance,def,target={},mapData={}){const explicit=qualityLevel(def?.cover?.quality);if(explicit!=='none')return explicit;const ratio=(objectTopFt(instance,def,mapData)-entityBaseElevationFt(target,mapData))/Math.max(.1,tokenHeightFt(target));return ratio>=.92?'full':ratio>=.62?'major':ratio>=.28?'partial':'none';}
function coverBetween(attacker={},target={},mapData={}){const core=coreRuntime();if(!core||layerOf(attacker)!==layerOf(target))return{level:'none',rank:0,objects:[]};let best='none';const objects=[];for(const instance of mapData.worldObjects||[]){if(core.objectLayer(instance)!==layerOf(target)||core.isDestroyed(instance))continue;const def=definitionFor(instance,mapData);if(!def?.affordances?.cover)continue;const rect=core.footprintRect(instance,def,mapData.grid||{}),interval=segmentRectInterval(attacker,target,rect);if(!interval||interval.enter<=.01||interval.enter>=.99)continue;const level=inferredCoverLevel(instance,def,target,mapData);if(!COVER_RANK[level])continue;objects.push({instance,definition:def,rect,level,rank:COVER_RANK[level],interval});if(COVER_RANK[level]>COVER_RANK[best])best=level;}return{level:best,rank:COVER_RANK[best],objects:objects.sort((a,b)=>b.rank-a.rank)};}
function distanceToRectPx(point={},rect={}){const x=Math.max(finite(rect.x),Math.min(finite(point.x),finite(rect.x)+finite(rect.width))),y=Math.max(finite(rect.y),Math.min(finite(point.y),finite(rect.y)+finite(rect.height)));return Math.hypot(finite(point.x)-x,finite(point.y)-y);}
function distanceToObjectFt(token={},instance={},def={},mapData={}){const core=coreRuntime();if(!core||layerOf(token)!==core.objectLayer(instance))return Infinity;return distanceToRectPx(token,core.footprintRect(instance,def,mapData.grid||{}))*feetPerPixel(mapData);}
function hidingSpotAt(token={},mapData={}){const core=coreRuntime();if(!core)return null;const hiddenId=token.stealthState?.hiddenInObjectId||token.hiddenInObjectId||null;for(const instance of mapData.worldObjects||[]){if(core.objectLayer(instance)!==layerOf(token)||core.isDestroyed(instance))continue;const def=definitionFor(instance,mapData);if(!def?.affordances?.hideInside)continue;const rect=core.footprintRect(instance,def,mapData.grid||{});if((hiddenId&&String(hiddenId)===String(instance.instanceId))||pointInsideRect(token,rect))return{instance,definition:def,rect,concealment:clean(def.hideSpot?.concealment||'full')||'full'};}return null;}
function hidingCandidatesNear(token={},mapData={},rangeFt=5){const core=coreRuntime();if(!core)return[];return(mapData.worldObjects||[]).flatMap(instance=>{if(core.objectLayer(instance)!==layerOf(token)||core.isDestroyed(instance))return[];const def=definitionFor(instance,mapData);if(!def?.affordances?.hideInside)return[];const distanceFt=distanceToObjectFt(token,instance,def,mapData);return distanceFt<=Math.max(0,finite(rangeFt,5))+EPS?[{instance,definition:def,distanceFt}]:[];}).sort((a,b)=>a.distanceFt-b.distanceFt);}
const occupantsFor=(id,mapData={})=>(mapData.tokens||[]).filter(token=>String(token.stealthState?.hiddenInObjectId||token.hiddenInObjectId||'')===String(id));
function canEnterHide(token={},instance={},def={},mapData={},rangeFt=5){const core=coreRuntime();if(!def?.affordances?.hideInside)return{valid:false,reason:'NOT_A_HIDING_SPOT'};if(!core||core.isDestroyed(instance))return{valid:false,reason:'HIDING_SPOT_DESTROYED'};if(def.affordances.openable&&instance.state?.open!==true)return{valid:false,reason:'HIDING_SPOT_CLOSED'};const distanceFt=distanceToObjectFt(token,instance,def,mapData);if(distanceFt>Math.max(0,finite(rangeFt,5))+EPS)return{valid:false,reason:'OUT_OF_RANGE',distanceFt};const capacity=Math.max(1,Math.trunc(finite(def.hideSpot?.capacity,1)));if(occupantsFor(instance.instanceId,mapData).filter(t=>String(t.id)!==String(token.id)).length>=capacity)return{valid:false,reason:'HIDING_SPOT_FULL',capacity};return{valid:true,reason:null,distanceFt,capacity};}
function applyHideState(token={},instance={},def={}){token.stealthState={...(clone(token.stealthState)||{}),hidden:true,hiddenInObjectId:instance.instanceId,concealment:clean(def?.hideSpot?.concealment||'full')||'full',hiddenSince:Date.now()};return token;}function exitHideState(token={}){token.stealthState={...(clone(token.stealthState)||{}),hidden:false,hiddenInObjectId:null,concealment:null,hiddenSince:null};return token;}
function coverAtToken(token={},mapData={}){const core=coreRuntime();if(!core)return{level:'none',rank:0,object:null,nearby:[]};const preferred=token.coverState?.objectId||token.preferredCoverObjectId||null,nearby=(mapData.worldObjects||[]).flatMap(instance=>{if(core.objectLayer(instance)!==layerOf(token)||core.isDestroyed(instance))return[];const def=definitionFor(instance,mapData);if(!def?.affordances?.cover)return[];const distanceFt=distanceToObjectFt(token,instance,def,mapData);if(distanceFt>5+EPS)return[];const level=inferredCoverLevel(instance,def,token,mapData);return[{instance,definition:def,distanceFt,level,rank:COVER_RANK[level]||0}];});nearby.sort((a,b)=>(String(a.instance.instanceId)===String(preferred)?-1:String(b.instance.instanceId)===String(preferred)?1:b.rank-a.rank||a.distanceFt-b.distanceFt));const best=nearby[0];return best?{level:best.level,rank:best.rank,object:best,nearby}:{level:'none',rank:0,object:null,nearby:[]};}
function physicalState(token={},mapData={}){const hiding=hidingSpotAt(token,mapData),cover=coverAtToken(token,mapData);return{zLayer:layerOf(token),elevationFt:entityBaseElevationFt(token,mapData),eyeHeightFt:eyeHeightFt(token,mapData),heightFt:tokenHeightFt(token),hidden:Boolean(token.stealthState?.hidden&&hiding),hiding,cover};}
return Object.freeze({EPS,DEFAULT_TOKEN_HEIGHT_FT,DEFAULT_EYE_HEIGHT_FT,COVER_LEVELS,COVER_RANK,layerOf,feetPerPixel,layerElevationFt,entityBaseElevationFt,tokenHeightFt,eyeHeightFt,tokenTopFt,objectBaseFt,objectTopFt,definitionsMap,definitionFor,pointInsideRect,segmentRectInterval,rayElevationFt,occlusionKind,occlusionBlocks,objectOccludersBetween,blocksLineOfEffect,qualityLevel,inferredCoverLevel,coverBetween,distanceToRectPx,distanceToObjectFt,hidingSpotAt,hidingCandidatesNear,occupantsFor,canEnterHide,applyHideState,exitHideState,coverAtToken,physicalState});});