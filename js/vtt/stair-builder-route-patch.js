(function(root,factory){const api=factory(root);if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root)root.LuminousVttStairBuilderRoutePatch=api;})(typeof window!=='undefined'?window:globalThis,function(root){'use strict';
function install(){const base=root?.LuminousVttStairRoute,builder=root?.LuminousVttStairBuilder;if(!base||!builder)return null;if(base.__stairBuilderAware===true)return base;const originalPointsFor=base.pointsFor.bind(base),originalRouteFor=base.routeFor.bind(base);function pointsFor(portal={},mapData={}){if(portal.type==='stairs'&&base.layoutOf(portal)==='straight'&&portal.builder){const points=builder.straightStepPoints(portal,mapData);if(points.length>1)return points;}return originalPointsFor(portal,mapData);}function routeFor(portal={},mapData={}){const route=originalRouteFor(portal,mapData);if(!route||route.layout!=='straight'||!portal.builder)return route;const points=pointsFor(portal,mapData);return{...route,points,pathLengthFt:base.pathLengthFt(points,mapData),builder:portal.builder};}const patched=Object.freeze({...base,__stairBuilderAware:true,pointsFor,routeFor});root.LuminousVttStairRoute=patched;return patched;}
const installed=install();return Object.freeze({install,installed});});